{
    "configurations": [
        {
            "type": "bashdb",
            "request": "launch",
            "name": "server-manager --help",
            "cwd": "${workspaceFolder}",
            "program": "${workspaceFolder}/_router.sh",
            "args": [
                "--debug",
                "--host-script",
                "${workspaceFolder}/sbin/server-manager",
                "server-manager",
                "create-user",
                "--help"
            ],
            "terminalKind": "integrated"
        }
    ]
}